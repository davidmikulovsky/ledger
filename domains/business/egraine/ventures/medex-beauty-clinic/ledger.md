# Medex Beauty Clinic — ledger

Newest first. Mirrors key entries from the source Decision Log; full detail stays in the source file.

---
**2026-07-23 (reconciliation, later same day)** — Per David's explicit instruction: `urgency` updated in Ledger from `high` to `medium`, matching the project's own record (lowered once Phase 1 closed). Both the `.git/index.lock` and `objects/maintenance.lock` found in the morning's audit are confirmed cleared — re-verified directly, repo clean and in sync with `origin/main`. Not addressed this pass, still open: the homepage hours-banner discrepancy, and the `type: sprint` vs `maintenance` question.

---
**2026-07-23 (full audit)** — David linked 4 candidate Medex folders and asked for a direct comparison before auditing. Found: `Documents/PROJECTS/Medex`, `Documents/PROJECTS_v0/Medex`, and the pen-drive copy are byte-identical to each other (README + decision log both match md5 exactly) — backup duplicates of the same pre-git ~07-10 snapshot, no git. `/Users/mklvsky/AI/PROJECTS/Medex` is git-initialized, active through today, and its own README self-declares it canonical — confirmed and used as source of truth. `source:` repointed (was still on the old pen-drive path). Found a huge amount of real progress not previously reflected: the original SEO sprint closed out, then a full Phase 1 "Truth & Consistency Pass" ran and closed 2026-07-23 with a new theme published live. **Discrepancy found on live re-check:** the project's own decision log claims the homepage's "Customer Support" hours banner was fixed and confirmed live, but fetching medex.fi directly (twice) still shows the old placeholder text — flagged, not resolved, not Ledger's call to fix inside Medex's own theme. **Priority/urgency drift found, not changed:** the project's own record now says `urgency: medium` (lowered once Phase 1 closed) against Ledger's `urgency: high` — flagged for David's explicit call rather than silently updated. **Type compliance flag:** `sprint` may no longer fit this now-cyclical Phase 1/Phase 2 shape (same pattern as Thann's `maintenance` type) — flagged, not changed.

---
**2026-07-23 (ADR-0012 rollout)** — Field migration: `parent_unit: business/egraine` renamed to `role: child` + `parent: business/egraine`, with `relationship: distributed-brand` added per ADR-0007's own language for this unit. Field-name fix only — no folder move, no change to what this unit is.

---
**2026-07-14** — Unit created in life-os, summarized from source Decision Log (D-001–D-016) and Task Tracker.

**2026-07-10** — Big sprint day: all 7 Quick Wins addressed (6 done, 1 partial), all 3 initial Strategic Investments done, all 5 Content Pages published, theme publish (SI-8) done. Contact page fixed and corrected (button color yellow→navy, D-016) but the corrected version is sitting in an active-draft theme, not yet published live — needs you to publish manually. Services page split into 11 individual treatment landing pages (D-015). Domain strategy finalized: single domain medex.fi, medexbeautyclinic.com to redirect (D-002, D-SI-2) — redirect itself not yet configured (needs DNS access). Cookie Policy separated from Privacy Policy as a real standalone page (D-012). product_type backfilled on 13 products (D-014). PageSpeed baseline captured (D-013) — Best Practices and mobile performance flagged as weak spots for later.
