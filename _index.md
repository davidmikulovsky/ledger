# Ledger — index

Rolled-up view across every domain. Regenerate by asking any time ("update the index") — it's derived from each unit's `_unit.md`, not maintained separately by hand.

_Last generated: 2026-07-23 (v0.6 Phase 0/1/3 shipped; Phase 2 reconciliation in progress — Thann/Egraine fixed; ADR-0012 rolled out: field migration + Eden's Eva/SkinThea converted to mentions:; MEMBER-MISMATCH/ORPHAN-PARENT shipped in ledger.py; guidelines-v2.1 committed and tagged)_

| Domain | Unit | Type | Status | Priority | Urgency | Last updated |
|---|---|---|---|---|---|---|
| business | egraine (parent, 4-pillar strategy — mentions: eden-eva, skinthea) | maintenance | active | high | medium | 2026-07-23 |
| business | egraine / medex-beauty-clinic | sprint | active | high | medium | 2026-07-23 |
| business | egraine / thann | maintenance | active | high | medium | 2026-07-23 |
| business | egraine / egraine-website-rebuild | sprint | active | high | medium | 2026-07-21 |
| projects | jascandi | maintenance | active | low | low | 2026-07-21 |
| projects | ledger (this framework, v0.6) | maintenance | active | medium | medium | 2026-07-22 |
| team | ai-lab (personal & team AI adoption) | maintenance | active | high | medium | 2026-07-23 |
| personal / career | — | — | no units yet | — | — | — |
| personal / health | — | — | no units yet | — | — | — |
| personal / nutrition | — | — | no units yet | — | — | — |
| personal / relationships | — | — | no units yet | — | — | — |
| personal / sport | — | — | no units yet | — | — | — |

Urgency values marked "proposed" in each unit's own `_unit.md` are Ledger's first read, not yet confirmed by David — same review pattern as priority.

## Governance

Decisions about Ledger itself live in `adr/` as ADRs (currently 12 — read-only boundary, local hosting, project-standard/tracking-protocol, status/priority vocabulary v1, domain restructure + vocabulary v2, ledger admin/reporting skill design, group representation & spin-off model, child-safety write boundary, git-lock & folder-access policy, canonical guidelines/shared-resources repo, scan engine & state model, family schema & delivery mechanism). How Ledger operates day to day is defined in the four guideline documents, now canonically maintained in the shared **ALFA+OMEGA** git repo (ADR-0010) — `project-standard.md` (including the canonical `status`/`priority`/`urgency` vocabulary), `tracking-protocol.md`, `workflow.md`, `checklists.md`; the copies under `guidelines/` here are superseded pointer stubs. Ledger's own spec/version history is in `SPEC.md` / `CHANGELOG.md` (currently v0.6) / `ROADMAP.md`.

## Needs your call directly (not just execution)

- **3 untracked on-disk projects (Ozuvox, project-cars, dives-enterprises)** — flagged since the original v0.6 self-audit, still not onboarded. David is correcting these repos himself first; Ledger will re-check and bootstrap with `new` once he says go. No action taken.

- **Urgency values across every unit** are Ledger's proposed first read (see each `_unit.md`'s "Urgency — proposed" section) — confirm or correct, same as priority was in v0.3.
- **Medex / Egraine hosting:** the pattern of "turn their docs folders into git repos, push to GitHub" is still logged as a want, but per ADR-0001 it can't happen as something Ledger initiates on its own — it would mean Ledger modifying those projects' own folders. Whenever you want this done, ask for it as its own explicit task, separate from tracking.
- **Eden's Eva** — dormant asset (edenseva.com), park with a holding page or scope a relaunch — deferred to "revisit after Q2" per the strategic plan, so no action needed yet, just don't forget it.
- **ai-lab — reconciled 2026-07-23, second pass.** First pass set `priority: medium`, which created a real `DRIFT` against AI-LAB's own README (still says `high`). Corrected: Ledger now inherits the project's own declared values — `priority: high`, `urgency: medium` — clearing the drift. Git re-verified clean (the one prior unstaged edit is now committed, no locks). Worth knowing: this is the one project with its own synced guidelines copy (`docs/guidelines/`), currently checked against v0.4 vocabulary, not yet re-checked against the new v2.1 stamp — not touched, that would be a write to AI-LAB's own repo.
- **Medex — full audit 2026-07-23, then reconciled later same day.** Source-of-truth confirmed (`/Users/mklvsky/AI/PROJECTS/Medex`, direct comparison against 3 duplicate/backup folders), `source:` repointed, a full completed Phase 1 caught up. Per David's instruction: `urgency` updated to `medium` (matching the project's own record) and both git locks confirmed cleared — re-verified directly. **Still open:** the homepage hours-banner discrepancy (project's own decision log claims a fix that two direct fetches this session don't show live) and whether `type: sprint` should become `maintenance`.

- **Egraine website rebuild:** legal pages (Privacy/Cookie/Terms) need a real lawyer review before they're trustworthy; contact form needs a real backend (currently a placeholder endpoint); Relationship Manager job posting needs your review (first draft, no source listing).
- **Thann — full audit 2026-07-23, then reconciled later same day.** Source repointed, `disputed` cleared (resolved at the source), massive real Phase 0/Phase 1 progress caught up. Git lock confirmed cleared and urgency drift resolved (THANN's own README now says `medium`, matching Ledger exactly) — both re-verified directly. Open: whether/when to publish the "Vessel — Working Copy" draft theme; 5 commits sitting unpushed (David's call).

- **Ledger v0.6 rollout:** Phases 0, 1, and 3 (ADRs ratified, scan engine + gap verbs shipped, dashboard shipped) are complete. Phase 2 (data reconciliation across the whole portfolio — not just Thann) is in progress, being worked by David directly rather than by Ledger. Phase 4 (automation) and Phase 5 (plugin packaging) are still ahead.
- **ADR-0012 family schema rollout, 2026-07-23 — closed out.** Closes the gap the uploaded proposal doc surfaced — ADR-0007's link-not-nesting decision existed only in `adr/`, which no tracked project reads, and its assumed field names never matched what `ledger.py` actually reads (`parent`). Fixed by locking canonical field names in ADR-0012, extending `project-standard.md`/`tracking-protocol.md` with a concrete schema, and bumping the ALFA+OMEGA guidelines stamp v2.0→v2.1 (committed and tagged `guidelines-v2.1` — David to push). Applied: `parent_unit:`/`sub_units:` renamed to `role:`/`parent:`/`members:` across the Egraine family; Eden's Eva and SkinThea converted from full units to the new `mentions:` tier (neither ever had real folder activity — their old unit files are preserved in `LEDGER/_to_delete/`, safe for David to delete natively); two new `ledger.py` checks shipped and smoke-tested against real data — `MEMBER-MISMATCH` (a parent's `members:` list disagrees with who actually declares `parent:` back) and `ORPHAN-PARENT` (a `parent:` reference to a unit that doesn't exist). **Explicitly declined per David's instruction:** placing a copy of the guidelines inside Egraine/Thann/Medex's own repos — "forbidden," so the `GUIDELINES-STALE` check still can't fire for them, and that stays as-is. **Still open:** whether to physically promote Thann/Medex to flat top-level units — see the "Promoting Thann/Medex — consequences" section in `egraine/_unit.md` for the actual tradeoffs; no functional benefit today since `ledger.py` already reads family fields regardless of nesting, but it does front-load the folder-move cost a future spin-off would otherwise force later.
- **Egraine — full audit 2026-07-23, health RED, needs your call:** (1) **egraine.fi is down** — SSL certificate error from a Coolify upgrade, confirmed independently this session, not just claimed. A replacement Astro site is built and a cutover plan is drafted but not executed — nothing is live yet; worth deciding when to act on the cutover plan given the current site is actively broken, not just stale. (2) `urgency: medium` has sat as Ledger's unconfirmed 07-21 proposal — a same-day proposal from David leans toward `high` given the confirmed outage; worth confirming. (3) The live `.git/index.lock` found at audit time is resolved — cleared and re-verified same day, repo clean and in sync with origin. (4) Two open proposals from the project's own 07-23 SEO strategy doc await your review (egraine.fi/medex.fi/thann.fi SEO relationship; Finnish-language coverage). (5) `egraine-website-rebuild` redundancy — the real site work lives directly in Egraine's own repo; worth a `report egraine-website-rebuild full` to confirm and decide on retiring/merging it. (6) **ADR-0007 promotion proposed but not applied** — a same-day proposal suggested swapping `sub_units:` for `portfolio_members:`/`parent:` links, but neither Thann nor Medex has actually been promoted yet (checked directly) — applying that swap would be the real restructuring itself, including folder moves, so it's queued for your explicit go-ahead rather than done as a side effect.
